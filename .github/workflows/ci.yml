name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ==========================================
  # JOB 1: TESTES DE SEGURANÇA
  # ==========================================
  security-tests:
    name: Security & Compliance Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Instalar pnpm
        run: npm install -g pnpm@10

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Verificar tipos TypeScript
        run: pnpm check

      - name: Executar testes de segurança
        run: pnpm test:security

      - name: Executar testes de saúde
        run: pnpm test:health

  # ==========================================
  # JOB 2: BUILD E TESTES
  # ==========================================
  build-tests:
    name: Build & Test
    runs-on: ubuntu-latest
    needs: security-tests
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Instalar pnpm
        run: npm install -g pnpm@10

      - name: Instalar dependências
        run: pnpm install --frozen-lockfile

      - name: Build do projeto
        run: pnpm build

      - name: Executar testes
        run: pnpm test
        continue-on-error: true

  # ==========================================
  # JOB 3: DEPLOY (apenas no main)
  # ==========================================
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    needs: [security-tests, build-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Deploy to Railway
        run: |
          echo "🚀 Deploy será feito automaticamente pelo Railway"
          echo "✅ Todos os testes passaram!"

      - name: Notificar sucesso
        if: success()
        run: |
          echo "✅ Pipeline concluído com sucesso em $(date)"
          echo "Deploy será acionado automaticamente pelo Railway"
