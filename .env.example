# ==========================================
# 🗄️ BANCO DE DADOS (OBRIGATÓRIO)
# ==========================================
# Endereço do banco MySQL onde os dados são armazenados
# Formato: mysql://usuario:senha@host:porta/nome_banco
# 
# Desenvolvimento local: mysql://root:suasenha@localhost:3306/elevare_db
# Produção (PlanetScale): mysql://user:pass@host.psdb.cloud/elevare_db?ssl={"rejectUnauthorized":true}
# 
DATABASE_URL=mysql://user:password@localhost:3306/elevare_db

# ==========================================
# 🔑 SEGURANÇA E AUTENTICAÇÃO (OBRIGATÓRIO)
# ==========================================
# JWT_SECRET: Chave secreta para tokens de autenticação
# ⚠️ CRÍTICO: Deve ter NO MÍNIMO 32 caracteres!
# Como gerar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ NUNCA use geradores online para produção! Gere sempre localmente.
# 
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_required

# ==========================================
# 🌐 OAUTH - AUTENTICAÇÃO DE USUÁRIOS (OBRIGATÓRIO)
# ==========================================
# Sistema de login via Manus OAuth
# Obtenha no Dashboard Manus: https://oauth.manus.im
# 
# OAUTH_SERVER_URL: URL do servidor OAuth (não altere)
OAUTH_SERVER_URL=https://oauth.manus.im

# VITE_APP_ID: ID único da sua aplicação no Manus
# Onde obter: Dashboard Manus > Criar Aplicação > Copiar App ID
VITE_APP_ID=your_app_id

# OWNER_OPEN_ID: Seu ID pessoal como proprietário
# Onde obter: Dashboard Manus > Meu Perfil > OpenID
OWNER_OPEN_ID=your_owner_open_id

# ==========================================
# 💳 STRIPE - SISTEMA DE PAGAMENTOS (OPCIONAL EM DEV)
# ==========================================
# Para ativar pagamentos e assinaturas
# Crie conta em: https://stripe.com
# 
# STRIPE_SECRET_KEY: Chave da API Stripe
# Teste: sk_test_... | Produção: sk_live_...
# Onde obter: Dashboard Stripe > Developers > API Keys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# STRIPE_PRO_PRICE_ID: ID do Plano PRO (R$ 67,00/mês)
# Onde obter: Dashboard Stripe > Products > Criar produto PRO > Copiar Price ID
STRIPE_PRO_PRICE_ID=price_your_pro_price_id

# STRIPE_PRO_PLUS_PRICE_ID: ID do Plano PRO+ (R$ 117,00/mês)
# Onde obter: Dashboard Stripe > Products > Criar produto PRO+ > Copiar Price ID
STRIPE_PRO_PLUS_PRICE_ID=price_your_pro_plus_price_id

# STRIPE_WEBHOOK_SECRET: Chave para validar eventos do Stripe
# Desenvolvimento: stripe listen --forward-to localhost:3000/api/stripe/webhook
# Produção: Dashboard Stripe > Webhooks > Copiar Signing Secret
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ==========================================
# 🤖 FORGE API - INTELIGÊNCIA ARTIFICIAL (OPCIONAL EM DEV)
# ==========================================
# APIs de IA para geração de conteúdo (e-books, prompts, anúncios)
# Necessário para funcionalidades de IA
# 
# BUILT_IN_FORGE_API_URL: URL da API Forge (não altere)
BUILT_IN_FORGE_API_URL=https://api.forge.manus.im

# BUILT_IN_FORGE_API_KEY: Chave de acesso à API
# Onde obter: Dashboard Manus > API Keys > Forge API
BUILT_IN_FORGE_API_KEY=your_forge_api_key

# ==========================================
# ⚙️ CONFIGURAÇÕES GERAIS (OPCIONAL)
# ==========================================
# NODE_ENV: Define o ambiente de execução
# Valores: development | production
# ⚠️ Em produção SEMPRE use: production
NODE_ENV=development

# PORT: Porta onde o servidor vai rodar
# Padrão: 3000
# Altere se já houver algo rodando nesta porta
PORT=3000

# ALLOWED_ORIGINS: Domínios permitidos a acessar a API (CORS)
# Formato: URLs separadas por vírgula (SEM espaços)
# Desenvolvimento: http://localhost:3000,http://localhost:5173
# Produção: https://seudominio.com,https://www.seudominio.com
# ⚠️ SEGURANÇA: Adicione APENAS domínios que você controla!
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ==========================================
# 📚 NOTAS IMPORTANTES
# ==========================================
# 1. JWT_SECRET deve ter NO MÍNIMO 32 caracteres para segurança
# 2. Em PRODUÇÃO, configure TODAS as variáveis obrigatórias
# 3. Para MVP mínimo, você precisa apenas de: DATABASE_URL, JWT_SECRET, OAuth (3 vars)
# 4. Stripe e Forge API são opcionais para começar (podem ativar depois)
# 5. NUNCA faça commit do arquivo .env no Git (já está no .gitignore)
# 6. Use senhas fortes e diferentes entre dev e produção
# 7. Leia o guia completo em: ENV_GUIDE.md
# 
# ==========================================
# 🔒 SEGURANÇA E LGPD
# ==========================================
# - Todas as senhas de usuários são hasheadas automaticamente
# - Use SSL/TLS em produção (HTTPS obrigatório)
# - Backups do banco devem ser encriptados
# - Logs não contêm dados sensíveis
# - Rate limiting ativo por padrão
# - CORS configurado para proteger a API
# 
# ==========================================
# 🆘 PRECISA DE AJUDA?
# ==========================================
# Leia o guia detalhado: ENV_GUIDE.md
# Documentação completa: README.md
# Guia de deploy: DEPLOY.md
